# connection_tasks

Custom Isaac Lab extension. Importing the package registers all gym tasks.

V0 task: `Connection-Open-Drawer-Franka-IK-Abs-v0` (Franka opening a drawer driven by a warp state machine).
