"""IsaacLab-side scene helpers (import only inside env_isaaclab).

These modules import `isaaclab` and therefore must NOT be imported on the
FoundationPose side or in plain unit tests. They contain no FoundationPose deps.
"""
