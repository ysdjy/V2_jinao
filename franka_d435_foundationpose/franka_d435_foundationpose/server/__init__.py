"""ZMQ pose server (FoundationPose side) and light client (IsaacLab side).

The client (pose_client) has NO FoundationPose dependency and is safe to import
inside env_isaaclab. The server (pose_server) must run in the foundationpose env.
"""
